<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">RaportOnline</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="nav flex-column">
        <div class="nav-item px-3">
@*             <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi-nav-menu nav-house-door-fill-nav-menu" aria-hidden="true"></span> Home
            </NavLink> *@
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link d-flex align-items-center gap-2" href="Raport">
                <span class="bi bi-journal-text fs-5" aria-hidden="true"></span>
                <span>Raport</span>
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link d-flex align-items-center gap-2" href="Wykres">
                <span class="bi bi-graph-up fs-5" aria-hidden="true"></span>
                <span>Wykres</span>
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
